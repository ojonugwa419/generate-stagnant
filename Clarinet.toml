[project]
name = "generate-stagnant"
authors = ["core-dev"]
description = "Decentralized workflow management for task tracking and team coordination"
telemetry = false
requirements = []
[contracts.stagnant-workflow]
path = "contracts/stagnant-workflow.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
